# built_version_annotation

Defines annotation used by [built_version](https://pub.dev/packages/built_version) to create a build time version string.

See the [example](example) to understand how to configure your package.