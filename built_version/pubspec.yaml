name: built_version
repository: https://github.com/huffSamuel/built_version/tree/main/built_version
description: Generate source code with the package's version by annotating a getter.
version: 1.2.0

environment:
  sdk: ^3.3.3

dependencies:
  analyzer: ^13.0.0
  build: '>=3.0.0 <5.0.0'
  glob: ^2.1.2
  meta: ^1.3.0
  pub_semver: ^2.0.0
  source_gen: ^4.0.1
  built_version_annotation: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  build_runner: ^2.4.0
  build_config: ^1.1.0
