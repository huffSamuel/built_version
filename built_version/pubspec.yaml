name: built_version
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.3.3

dependencies:
  analyzer: ^6.4.1
  build: ^2.4.0
  glob: ^2.1.2
  meta: ^1.15.0
  pub_semver: ^2.1.4
  source_gen: ^1.5.0
  built_version_annotation: ^1.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  build_runner: ^2.4.0
  build_config: ^1.1.0
